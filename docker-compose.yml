version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=mysql+pymysql://root:ruMXfGriDMZnIEKgcsAYkKUgzJlJrLQN@nozomi.proxy.rlwy.net:56420/railway
      - DEBUG=true
      - SECRET_KEY=dev_secret
      - JWT_SECRET_KEY=jwt_secret
    restart: always

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        - VITE_API_BASE_URL=http://localhost:8000/api/v1
        - VITE_BACKEND_BASE_URL=http://localhost:8000
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: always
